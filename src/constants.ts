import {BasicType} from './types/basic';

export const nullType = new BasicType({name: 'null'});
export const undefinedType = new BasicType({name: 'undefined'});
export const booleanType = new BasicType({name: 'boolean'});
export const numberType = new BasicType({name: 'number'});
export const stringType = new BasicType({name: 'string'});
